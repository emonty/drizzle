# Drizzle test suite — known failures on the Ubuntu Precise base image.
#
# Format: one test name per non-blank, non-comment line. Inline `#` comments
# are allowed. The run-tests.sh wrapper exits 0 when DTR's failure set is
# fully contained in this list — so this file is the contract for "what
# we've already accepted as broken on this phase."
#
# Entries should never just say "broken" — record *why*. Future phases get
# their own skiplist files (e.g. skiplist.trusty.txt) and the diff against
# this baseline is the gate.

json_server.basic    # needs curl in the test image; not yet installed
rabbitmq.variables   # needs a running rabbitmq-server/rabbitmqctl; not yet installed
main.comments        # `/*!72301 ... */` version comment parsed as live SQL; real parser bug, fix in a later phase
